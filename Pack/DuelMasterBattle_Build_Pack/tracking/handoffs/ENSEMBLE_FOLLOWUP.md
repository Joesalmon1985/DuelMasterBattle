# Ensemble follow-up (not part of fix/g04-retained-ui)

Keep these commits on `BuildPackV03-ensemble-depth-pass` for a later PR:

- `58ea2d2` feat(dialogue): deepen ensemble profiles, relationships and scene contracts
- `6f7be0f` feat(dialogue): add bounded offline ensemble LLM authoring pilot

They were intentionally excluded from the G04 repair merge (main already has the earlier ensemble authoring via PR #7).
