# G08 — Narrative breadth, presentation and authoring tools

**Stop point:** T142.  
**Suggested session:** 30–45 minutes.  
**Scenario:** Content sample manifest.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G08` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G08/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Play the selected sample across all eight quest families, at least two eras and two different dungeon layouts.
2. Read a displaced/dead-target/world-resolved/full-cycle branch. Look for repeated phrasing, leaked facts and choices with no effect.
3. Try the four rival samples, later-era art/sound and scaled labels/reduced motion.
4. Use the village panel to load a seed, choose a content pack, preview a changed line and export a failure bundle.

## Expected observations

- People and problems differ in cause and consequence, not only names.
- Offline lines remain truthful and readable through state changes; required objects/paths remain recoverable.
- Art/sound make era, role, danger and interaction clear; controls are usable with one pointer.
- The village testing workflow actually helps you add/review dialogue using the normal engine.

**Experience question:** Is the broader content engaging and practical for you to extend?

## Record the result

Reply with `G08 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G08 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
