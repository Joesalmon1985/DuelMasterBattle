# G02 — Autonomous factions, construction and carts

**Stop point:** T048.  
**Suggested session:** 15–20 minutes.  
**Scenario:** FX-CARGO.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G02` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G02/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Load the supplied construction/trade scenario. Inspect the producing warehouse and its named cart.
2. Advance turns through Travel/Wait while watching a real delivery and a construction completion.
3. Use the supplied blocked-route scenario and then its normal route-clearing interaction. Check the goods do not teleport.
4. Watch a full round of faction seats and one technology pick per faction; save with a cart loaded and resume.

## Expected observations

- The cart carries the same cargo before/after the save; delivery is delayed by road distance and blockage.
- A settlement appears only after its real costs arrive; military victory never grants ownership here.
- Factions choose actions autonomously; your controls do not assign workers or armies.
- Trade/technology feedback is understandable without forcing a management dashboard.

**Experience question:** Can you see why goods and construction are delayed?

## Record the result

Reply with `G02 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G02 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
