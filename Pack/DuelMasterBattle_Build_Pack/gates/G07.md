# G07 — All eras and repeated historical cycles

**Stop point:** T132.  
**Suggested session:** 25–35 minutes.  
**Scenario:** FX-CYCLE.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G07` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G07/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Use the supplied legitimately generated checkpoints to walk through Historic, Modern and dystopian Future versions of a place.
2. Try pollution cleanup, alien response and Future catastrophe manifestations. Check old typed hazards are still named correctly.
3. Cross Future→Prehistoric. Revisit a known person, quest, dropped object and authored relic location.
4. Review known Chronicle history and a sole-faction split example; also start a normal six-faction world.

## Expected observations

- Each era has a clear identity and reachable responses to surviving hazards.
- New-cycle political competition resets; a future army does not dominate the new Prehistoric contest.
- Living people, wizard memory, active promises and defined relics persist without misleading text.
- Six factions and solo recovery remain playable; known history is intelligible.

**Experience question:** Does history feel like the same changing world across a complete cycle?

## Record the result

Reply with `G07 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G07 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
