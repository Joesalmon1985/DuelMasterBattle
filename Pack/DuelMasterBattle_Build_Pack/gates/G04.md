# G04 — Battles, wizard power and catastrophe

**Stop point:** T076.  
**Suggested session:** 20–30 minutes.  
**Scenario:** FX-BATTLE + FX-HAZARD.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G04` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G04/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Watch a local fight. Cast destruction at one friendly, neutral and hostile eligible unit/building in separate supplied saves; try a group or remote target.
2. Apply each support buff; pause or duel while a buff is active. Leave an ongoing battle and return.
3. Treat three distinct adjacent affected hexes. Try the same hex again, Wait, save/reload, and try again.
4. Run the near-outbreak scenario and observe warnings, source/cargo blockage, eligible treatment and final terminal loss.

## Expected observations

- Local range/terrain matter; you can change the fight without issuing army orders. Faction soldiers cannot harm you.
- Destruction has real cargo/industry/person consequences; departing does not restore casualties.
- One successful treatment per adjacent hex is enforced across Wait/reload; duels freeze world time.
- The terminal cascade is understandable before the menu return; ordinary duel failure is nonterminal.

**Experience question:** Do intervention and competing threats feel powerful and readable?

## Record the result

Reply with `G04 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G04 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
