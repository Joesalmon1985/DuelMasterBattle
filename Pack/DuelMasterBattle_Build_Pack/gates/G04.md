# G04 — Battles, wizard power and catastrophe

**Stop point:** T076 (plus G04 reuse repair U01–U08 when FIX_REQUIRED).  
**Suggested session:** 20–30 minutes.  
**Scenario:** FX-BATTLE + FX-HAZARD.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour. When repairing retained duel / shared interaction, follow `docs/DuelMasterBattle_G04_Reuse_Repair_Addendum.md` (U01–U08) and the G04 section of `EXECUTION_AMENDMENTS.md`. The earlier Interaction Repair Brief is superseded where it conflicts.
- Provide the exact checkout commit, playable launch commands, fixture seed/save and **labelled reset** instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G04` through the mapped wrapper. Include actual unit/integration/schema/Godot results. A headless parse alone is insufficient. **Set AWAITING_HUMAN only after required automated checks pass. Do not treat automated PASS as gate approval.**
- List known defects and which steps they affect.
- Save this packet under `tracking/gates/G04/`, set AWAITING_HUMAN and STOP. Do not begin T077 while this gate is pending.

## Linux launch (after reset)

```bash
bash tools/play_g04_battle.sh
bash tools/play_g04_hazard.sh
```

Use the packet reset so an old fixture save does not obscure the repair.

## Joe's playtest checklist

| Check | Expected behaviour |
|---|---|
| Enter the battle | One faction owns the settlement and its active factories. Opposing faction has invading soldiers. Unit shapes identify types; colours and labels identify factions. |
| Move immediately | Walk away from spawn in every direction without getting trapped. Controls remain responsive while units fight. |
| Click a distant unit | A description appears, labelled something like `Red Skirmisher`. Clicking it does not move the wizard or cast anything. |
| Approach and click again | An attached choice card appears with Observe / Buff… / Destroy, plus a way to close it. The permanent spell toolbar is gone. |
| Observe a soldier | After a deliberate Observe, the label gains a stable individual name while retaining faction/type. A second unobserved soldier remains unnamed. |
| Open the choices | Combat and Game Time pause while you choose. Closing resumes them without jumping forward. World Turn does not increase. |
| Choose a buff | You can choose Shield, Attack speed or Range. The selected unit shows a clear effect or confirmation identifying the buff. |
| Choose Destroy | That individual unit dies. Other units remain unaffected. The destruction survives saving and loading. |
| Cancel or walk away | Keyboard, pad or ground movement closes the card; nothing is cast; movement works normally. |
| NPC Talk | Nearby NPC Talk uses the familiar village speech/response presentation with an authored line and reply — not a HUD-only “Interacted” message. |
| Enter the hazard test | Three labelled manifestations appear at three different boundary approaches. They remain visible while you walk and wait. |
| Click a hazard | From a distance you get an observation. Nearby, an eligible hazard offers Challenge. |
| Accept Challenge | The retained full duel opens (`game_board` + `DmbBattleSim`): spell icons, Ward setup, rival casting, feedback and histories. Seeing only four colour-cycling buttons and Guess/Resign means the repair failed. |
| Finish the duel | Winning removes the selected cube only. Losing does not cause Game Over. The world resumes correctly, and treatment allowances persist across Wait/save/load. Duplicate completion does not remove another cube. |
| Resize and reload | Portrait and landscape keep choices and controls accessible. Reload preserves learned identities and the world changes you made. |

## Decisive criterion

Do not accept G04 merely because automated checks pass. The decisive test is whether you can walk up to a moving soldier, click it, choose a spell naturally, and see the correct persistent result—and then Challenge a hazard into the familiar full Ward duel.

**Experience question:** Do intervention and competing threats feel powerful and readable?

## Record the result

Reply with `G04 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G04 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected work, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits. It must not broaden the design or silently mark this gate passed from automated results.
