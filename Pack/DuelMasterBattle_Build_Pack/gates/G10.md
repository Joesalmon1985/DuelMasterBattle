# G10 — Packaged full-baseline acceptance

**Stop point:** T160.  
**Suggested session:** 45–60 minutes.  
**Scenario:** FX-RELEASE + normal full start.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G10` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G10/`, set AWAITING_HUMAN and STOP. T160 stays WAITING_HUMAN until this final acceptance is recorded.

## Joe's playtest

1. On the target Windows machine, unzip and launch from a folder with spaces using no Python/server/developer setup. Play with internet disconnected.
2. Start a normal six-faction game; operate movement, dialogue, magic, inventory, Wait and saves with one pointer.
3. Use the supplied release checkpoints to verify quest/duel/battle/era/full-cycle continuation, then reload a mid-encounter save.
4. Quit/relaunch, move the installation folder, test backup recovery and confirm no lingering sidecar. Review release notes and any declared remaining limitations.

## Expected observations

- The exact packaged build works offline on Windows, including local data and trained policies.
- All required baseline features are included; no required placeholder or untrained network is hidden behind a completed checkbox.
- No duplicate goods/rewards/people/units arise from saves, transfers or transitions.
- You accept the named build and its honest remaining optional-extension scope.

**Experience question:** Do you accept this exact build as the completed baseline game?

## Record the result

Reply with `G10 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G10 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
