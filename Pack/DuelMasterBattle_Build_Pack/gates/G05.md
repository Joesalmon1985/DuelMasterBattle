# G05 — Fully explorable Prehistoric world

**Stop point:** T096.  
**Suggested session:** 30–40 minutes.  
**Scenario:** FX-VILLAGE (full board; **no active quest**).  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## What G05 is now

Generate the complete Prehistoric strategic board and let John walk every
ordinary strategic node through one standard LocalArea contract.

- 19 hexes / 54 nodes / 72 edges
- Every node projects to the same playable footprint (48×48)
- Geography from touching hexes; exits from real adjacency
- Settlements use the same projector; normal starting-core industry
- **No shortage quest, demon, sluice, Route A/B in baseline play**

Archived prototype quest content remains under `FX-VILLAGE-QUEST` /
`content/source/quests/shortage/` for a later narrative milestone. Generic
QuestService / puzzle / duel frameworks stay in the architecture unused.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, playable launch command, fixture seed and reset instructions.
- Run `python tools/check.py --gate G05` through the mapped wrapper. Include 54-node projection/travel tests and G01–G04 regressions.
- List known defects. Optional historical quest hints belong in an archived section, not the main playtest.
- Save this packet under `tracking/gates/G05/`, set **AWAITING_HUMAN** and STOP. Do not begin T097 / G06 while this gate is pending.

## Joe's playtest

1. Start inside the real home settlement (`bash tools/play_g05.sh`). Walk the civic core and resource edges.
2. Leave via a visible path/road. Cross several wilderness nodes. Follow a constructed road when you find one.
3. Reach a second faction settlement. Confirm its buildings, people and economy are real — not an empty shell.
4. Take a different route home. Confirm the original settlement geography is unchanged/persistent.

## Expected observations

- The whole board exists as one world; every node is the same LocalArea size.
- Empty nodes feel like countryside/crossroads, not debug rooms.
- Roads appear only on constructed strategic edges; other adjacencies are trails.
- No quest banner, Mara shortage dialogue, demon cube or sluice entrance in baseline.
- People/units/carts appear only where the simulation places them.

**Experience question:** Does this feel like a world you can explore, or a single village level?

## Record the result

Reply with `G05 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G05 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video and reproduction step are enough.

Only Joe's explicit PASS clears this human gate. Agents must not self-PASS from automated results.
