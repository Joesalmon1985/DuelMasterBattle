# G05 — The procedural village and quest test

**Stop point:** T096.  
**Suggested session:** 30–40 minutes.  
**Scenario:** FX-VILLAGE.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G05` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G05/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Start the generated village without reading the solution hint. Talk to the existing factory worker and explain in your own words why work has stopped.
2. On one save, investigate and use the demon-duel solution. Leave and return; check actual resumed work and the worker's response.
3. On a fresh save, find the sluice dungeon, use its clues/items/mechanisms and restore the alternate route. Confirm the dialogue does not falsely say the demon was removed.
4. Try the world-resolved and destroyed-target saves. Drop/recover the required item. Save and reload during a conversation, puzzle and duel.

## Expected observations

- Both solutions use real world causes and restore actual output; they do not merely change quest text.
- The same worker, buildings, items and quest persist after travel/save.
- Clues and controls are fair; failed checks have coherent consequences and no reopen-to-farm loop.
- World repairs, displacement or death produce truthful continuations and no resurrected target.

**Experience question:** Would you want to meet these people and solve another such problem? This is the village-testing milestone.

## Record the result

Reply with `G05 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G05 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
