# G01 — Local controls, clocks and reliable startup

**Stop point:** T024.  
**Suggested session:** 10–15 minutes.  
**Scenario:** FX-CLOCK.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G01` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G01/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Launch using the agent-provided command or executable. Move around the local area with one pointer; inspect a distant label, approach and interact.
2. Travel one adjacent node, then press Wait once. Hold the button briefly and confirm it does not repeat. Try an invalid exit.
3. Open a pause screen, lose/restore window focus, and resume. Save, close completely, relaunch and load.
4. Use the supplied controlled bridge-failure button in this development build; confirm the game pauses and recovers honestly.

## Expected observations

- Movement and labels are usable; the wizard does not jump to a destination before acknowledgement.
- Accepted Travel and Wait each add one turn; invalid/held input adds none.
- Paused/focus/closed time grants no production or extra turns; loaded position is correct.
- Recovery clearly reports any rollback and does not double an action or leave a background process.

**Experience question:** Can you operate the basic world and trust pause/save/startup?

## Record the result

Reply with `G01 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G01 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
