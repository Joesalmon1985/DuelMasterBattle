# G09 — Trained faction leadership

**Stop point:** T150.  
**Suggested session:** 20–30 minutes plus review of evaluation summary.  
**Scenario:** Held-out policy replays.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G09` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G09/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Read the agent's paired evaluation summary: baseline and candidate counts, seed set, legality, catastrophe, win performance and runtime timing.
2. Watch representative build/trade/war decisions from each of three accepted policies. Ask why a surprising decision was legal.
3. Play a normal segment with the selected policy library, then save/reload to verify stable policy identity.
4. Inspect an inference-failure demonstration and verify legal heuristic fallback without a free extra action.

## Expected observations

- There are three actual qualified trained artifacts with provenance and measured differences.
- Policies obey costs/knowledge/activation rules and do not worsen the required catastrophe metric.
- Behaviour is credible and different enough to be useful; a model filename is not the only distinction.
- Failure/replay is robust and requires no runtime internet.

**Experience question:** Are these policies ready to become the default leadership library? If not, record a specific behaviour or budget constraint.

## Record the result

Reply with `G09 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G09 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
