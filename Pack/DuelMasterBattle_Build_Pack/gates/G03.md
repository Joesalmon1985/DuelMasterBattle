# G03 — Industry and persistent workers

**Stop point:** T058.  
**Suggested session:** 15–20 minutes.  
**Scenario:** FX-INDUSTRY.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G03` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G03/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Watch the fixture for 100 unpaused seconds. The shared route should complete one of each of the three unit types.
2. Stand in a decorative worker path. Check the worker adapts visually while production continues.
3. Use the provided real building-damage/strike scenario, then its paid repair or valid cause-removal action.
4. Leave and return to the worker, then save/load during partial production. Compare their name/job and the resumed meter in developer evidence.

## Expected observations

- Actual shortage/damage changes work/output; a walking obstruction does not.
- People remain the same after returning; a changed job has a separately named replacement.
- Off-screen settlements continue producing during unpaused time.
- Street activity and NPC feedback help explain the actual bottleneck.

**Experience question:** Is watching the village useful and interesting, and can you diagnose its problem?

## Record the result

Reply with `G03 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G03 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
