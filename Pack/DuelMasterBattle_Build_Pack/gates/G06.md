# G06 — Integrated MVP through Historic transition

**Stop point:** T114.  
**Suggested session:** 45–60 minutes; use supplied near-transition save for focused recheck.  
**Scenario:** FX-ERA + normal MVP start.  
**Initial status:** NOT_READY. Nothing in this pack is a completed playtest.

## Agent preparation before asking Joe

- Complete this phase's task implementations and automated checks, preserving all prior accepted behaviour.
- Provide the exact checkout commit, rules/content hashes, playable executable or verified launch command, fixture seed/save and reset instructions. Fill in actual paths; do not give Joe a command that has not launched successfully.
- Run `python tools/check.py --gate G06` through the mapped wrapper. Include actual unit/integration/schema/Godot results and relevant scenario replay. A headless parse alone is insufficient.
- List known defects and which steps they affect. Include optional solution hints in a separate clearly labelled section so Joe can try inference first.
- Save this packet under `tracking/gates/G06/`, set AWAITING_HUMAN and STOP. Do not begin the following task while this gate is pending.

## Joe's playtest

1. Start the normal two-faction sandbox and follow an optional local goal: observe, talk, travel, intervene and return. Use normal controls only.
2. Experience a real cart delivery, industrial production, a duel/dungeon and a local conflict or hazard.
3. Continue toward the first 10-VP transition. If pace prevents reaching it in the session, record that as pacing evidence and inspect the provided legitimately reached checkpoint.
4. Visit the transformed Historic core and a surviving legacy site. Find a continuing person/quest and reload the post-transition save.

## Expected observations

- The game loop works as an integrated game, not separate demo scenes.
- Exactly one immediate transition occurs, with inert collapse ruins and working new cores.
- People/relationships/active quests remain; old non-core industry produces old units and can later be paid-upgraded.
- The MVP demonstration does not impose a fake full-game ending.

**Experience question:** Is the core game enjoyable enough to justify scaling it?

## Record the result

Reply with `G06 PASS — <build/commit>` if the required behaviour is satisfactory. Otherwise reply `G06 FIX_REQUIRED — <what happened and what you expected>`. A short screenshot/video, save and reproduction step are enough; the agent should retrieve detailed logs itself. If the machine/build cannot run, use BLOCKED and describe the immediate obstacle.

The agent records who accepted, exact build/manifest, date, steps tried and observations. Only Joe's explicit PASS clears this human gate. On FIX_REQUIRED it reopens affected tasks, adds a meaningful regression, repairs and reruns the relevant checks, then resubmits the affected steps. It must not broaden the design or silently mark this gate passed from automated results.
