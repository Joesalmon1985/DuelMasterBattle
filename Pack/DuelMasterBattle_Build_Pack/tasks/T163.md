# T163 — Validate all runtime-visible content against the registry

**Phase:** Overnight G11/G12 extension  
**Prerequisite:** T162  
**Human gates required:** none invented; owner review via `AUTO_READY_FOR_OWNER_REVIEW`  
**Source:** EXECUTION_AMENDMENTS overnight G06→G12 section; gates/G11.md

## Implement

Validate all runtime-visible content against the registry. Prefer semantic placeholders. Do not invent human PASS. Record receipt `tracking/handoffs/T163.json`.

## Acceptance

Mapped `python tools/check.py --task T163` returns nonzero only on real failure. Evidence under `tracking/gates/`.
