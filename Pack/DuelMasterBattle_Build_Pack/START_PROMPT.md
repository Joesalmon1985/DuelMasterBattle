# Prompt to give the coding agent

The DuelMasterBattle_Build_Pack folder is available alongside my game checkout.

Implement this pack methodically. Begin with AGENT_START_HERE.md, tracking/progress.json and the first unmet task. On a fresh run that is T001. Audit the real checkout and map the proposed paths before modifying code. Preserve existing compatible code and my uncommitted work.

Work one small task at a time, implement its complete contract, run its specified checks, write its evidence receipt, and continue automatically to the next task. Use the task cards and named contract files as your working context; do not repeatedly ingest the whole design document. Normal play, village fixtures and training must use the same authoritative game rules.

Stop at each required manual playtest gate with a runnable build, exact launch instructions, seed/save and the gate's checklist. Wait for my explicit PASS before proceeding. If a test remains unexplained after three focused repairs, give me a small reproducible blocker report rather than looping indefinitely.

The goal is the full baseline game through G10, including the integrated MVP at G06, four eras and repeated cycles, full recipe/content coverage, qualified trained policies and the offline desktop package. Do not quietly stop at the MVP or mark unexecuted tests, placeholder features or untrained networks complete.

First tell me which checkout/branch you found and the current task, then start the work.
