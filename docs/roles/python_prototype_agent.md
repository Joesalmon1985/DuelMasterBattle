# Python Prototype Agent Role

## Responsibility
Build the first clickable version with pure logic separated from UI.

## For this project
- Pure logic: `python_prototype/duel_mastermind/` (no tkinter imports)
- UI: `python_prototype/duel_mastermind/ui/` (tkinter only)
- Keep **deliberately minimal** — fast proof of rules + flow
- If tkinter work threatens Godot slice, stop at functional prototype and prioritise M3/M4

## Deliverables
- pytest green + fixtures generated
- README with run instructions
